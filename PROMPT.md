# 项目概述

- 名称：Arcol Protocol
- 描述：一个定制化的、部署在私有服务器上的 个人信息整合和展示的全栈应用，提供基于Flutter的WebUI和REST API接口，支持多平台访问。

# 核心功能

为了方便用户在本地服务器上部署和使用，系统设计为单用户模式，所有数据和功能均围绕该用户进行管理和展示。

## Redis 队列

  - 设计一个统一的 Redis 队列系统，供各个模块中的所有推送需求使用。
  - 提供一个配置界面，让用户设置消费者服务对应的进程、Redis 端口和队列名称，并提供测试连接功能。设置之后推送功能才会工作。 
  - 提供一个内置的邮件系统消费者，但也保留用户自己编写消费者的空间。  

## 功能模块

### AI API模块

- 为其他模块的AI功能提供纯文本推理模型支持，负责管理所有与 AI 相关的第三方 API 密钥（OpenAI格式，支持自定义提供商），并提供统一的接口供其他模块调用。

### 时间管理模块

- 提供待办事项和时间追踪功能：待办事项除增删改查之外，还支持优先级设置和截止日期提醒；时间追踪功能用于记录用户专注时间，在webui中提供便捷的开始/结束和事项录入，并支持自动统计和可视化展示。
- 增删改查、时间追踪等提供对外API接口，截止日期提醒在用户设定的时间推入指定Redis 队列。

### 资产监控模块

- 使用交易平台API监控用户所选的数字资产变化，提供K线图等可视化展示。该部分可以仅完成基本框架，API接入和平台适配在后续更新中完成。
- 提供AI辅助的资产分析和投资建议功能，使用用户的资产组合、市场数据，并从RSS订阅系统拉取近期新闻，组合成材料发送到AI模块，生成个性化的投资建议报告。

### 站点路由模块

该模块旨在提供对用户本地服务器上运行的各种服务的统一访问入口和管理界面，仅需完成重定向和状态监控功能（反向代理由用户自己完成，系统不需要提供）：
- 在WebUI中提供一个可视化的站点路由管理界面，用户可以通过拖拽方式添加、编辑和删除站点路由，并支持分组和标签功能。
- 每个站点可以设置其在本地服务器中对应的进程或docker容器（通过进程名、Docker 容器 ID、Docker SDK查询等方式绑定），系统会自动监控这些进程的状态（是否存活、CPU内存占用等），并提供开关和日志查看等维护功能。

### 服务器信息和本系统监控模块

- 实时监控服务器的CPU、内存、磁盘使用率等关键指标，并提供可视化的仪表盘展示。
- 监控本系统的运行状态，包括各个模块的健康状况、API请求统计等，并提供日志记录和错误追踪功能（日志建议集成成熟工具）。
- 支持设置阈值和自动报警功能，当服务器资源使用超过设定的阈值时，通过推入指定Redis 队列发送。

### RSS订阅和推送系统

- 提供一个RSS订阅管理界面，用户可以添加、编辑和删除RSS订阅。系统拉取订阅内容并实时推送，同时持久化存储订阅内容和分析结果。
- 对于获取的RSS内容，提供基于AI的内容分析、摘要、分类和重要程度评估功能。若重要程度大于设定阈值，将处理后的结果推送到指定的Redis队列。


## WebUI 视图

注意，以下描述所提到的**用户** 均为系统的单一用户，**访客** 是指未认证的访问者。

| 视图/页面名称 | 访问权限 | 主要功能模块 | 数据来源 / 更新方式 | 实时性要求 | 备注 |
|--------------|----------|--------------|---------------------|------------|------|
| **访客面板 (Guest)** | 公开（无需认证） | - 用户当前专注事项及已用时间<br>- 用户数字资产总览及近期变化百分比<br>- 站点路由状态和简易访问统计<br>- RSS 最新摘要（标题+分析结果） | - 轮询 REST API（短轮询，例如 30 秒）<br>- 可选 WebSocket 接收专注状态变化 | 低：分钟级更新即可，专注时间可每秒刷新 | 作为公开大屏或信息面板使用，仅展示关键信息，不可操作。 |
| **仪表盘 (Dashboard)** | 需认证（用户） | - 所有模块的状态卡片（可自定义）<br>- RSS 订阅内容及详细分析<br>- 服务器资源监控图<br>- 资产走势图<br>- 待办事项概览 | - REST API 获取静态数据<br>- WebSocket 接收实时状态（如 CPU 报警）<br>- 支持拖拽布局，配置保存在后端 | 中：部分数据（如服务器指标）可 5 秒刷新一次 | 用户登录后的主界面，可自定义布局。 |
| **时间管理视图** | 需认证（用户） | - 待办事项 CRUD + 优先级/截止日期设置<br>- 时间追踪（开始/暂停/结束）<br>- 专注统计图表 | - REST API（增删改查）<br>- WebSocket 推送专注状态变化 | 高：专注计时需秒级同步 | 独立的配置页，从仪表盘或导航进入。 |
| **资产监控视图** | 需认证（用户） | - 资产列表（添加/删除交易所及币种）<br>- K 线图及技术指标<br>- AI 分析报告生成与查看 | - REST API（资产配置、报告拉取）<br>- 调用第三方交易所 API（后端代理） | 中：K 线图可缓存 1 分钟 | 资产配置页，分析报告可能需等待后台任务生成。 |
| **站点路由视图** | 需认证（用户） | - 站点列表（拖拽排序、分组、标签）<br>- 站点与本地进程/容器关联配置<br>- 进程启停控制、日志查看 | - REST API（路由配置）<br>- WebSocket 推送进程状态变化（如 crash） | 高：进程启停需立即反馈 | 相当于一个轻量级容器管理面板。 |
| **服务器监控视图** | 需认证（用户） | - 服务器实时指标（CPU/内存/磁盘）<br>- 阈值设置界面<br>- 系统日志查看与错误追踪 | - REST API（阈值设置）<br>- WebSocket 推送报警和实时指标 | 高：监控数据至少每秒一次 | 可选用 Grafana 风格仪表盘，但需自行实现。 |
| **RSS 管理视图** | 需认证（用户） | - RSS 订阅源 CRUD<br>- AI 分析结果展示（摘要、分类、重要性）<br>- 推送历史查看 | - REST API（订阅管理、分析结果）<br>- 后台任务生成分析报告 | 低：新内容在后台更新后推送到前端即可 | 重要性阈值等设置也在此界面。 |
| **系统设置视图** | 需认证（用户） | - AI 模块 API 密钥管理<br>- 通知方式配置（邮件/Webhook）<br>- 用户偏好（主题、语言） | - REST API（设置项保存） | 无实时性要求 | 可合并为一个通用设置页。 |

# 技术栈

- Flutter前端 + python后端 + PostgreSQL + Redis 中间件

# 部署

- 设计为单用户模式，部署在用户的私有服务器上，支持 Docker Compose 一键部署。

# 数据维护

- 设计一个统一的数据维护和备份机制，定期将 PostgreSQL 数据库和 Redis 数据进行备份，并提供恢复功能。
  
# 鉴权

- API 使用 API token ，前端使用 JWT 存储凭证，提供登录界面进行认证。

# 产出

验收成果：

1. 详细的功能设计文档。
2. 系统前后端各个模块的具体实现。
3. API提供 OpenAPI/Swagger 文档，便于后续客户端代码编写。

# 其他要求

涉及关键数据流和算法的，需要编写单元测试，确保核心功能的正确性和稳定性。

# 约定
## Flutter
- 命名：snake_case 文件，Pascal_case 类
- 禁止使用 setState 作为主要状态管理方式
- 禁止在 StatelessWidget 中手动管理状态
- 禁止使用 ChangeNotifier 但未正确 dispose
- 禁止async 方法不返回 Future<void> 而返回 void
- 禁止在 build() 方法中直接调用 async 方法
- 禁止硬编码字符串/颜色/尺寸（必须用 Theme/常量）
- 禁止嵌套超过4层的 Column/Row（必须用自定义 widget 拆分）
- 禁止单一文件超过 300 行
- 禁止在 lib/ 根目录直接放业务代码（必须分目录）
- 公开 API 的变量必须显式声明类型
## Python
- 禁止直接拼接 SQL（必须用参数化查询）
- 必须使用异步任务队列（如 Celery、RQ）处理耗时操作，避免阻塞事件循环
- 禁止缺少日志记录的关键业务逻辑